<configuration>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss} - %highlight(%msg) %n
            </pattern>
        </encoder>
    </appender>

    <logger name="tbank.kudago" level="ERROR" />
    <logger name="tbank.kudago" level="INFO" />
    <logger name="tbank.kudago" level="WARN" />
    <logger name="tbank.kudago" level="DEBUG" />

    <root level="INFO">
        <appender-ref ref="STDOUT" />
    </root>
</configuration>